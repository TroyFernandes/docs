services:
  zensical:
    container_name: docs
    restart: unless-stopped
    image: zensical/zensical:latest
    ports:
      - "8005:8000"
    volumes:
      - ./:/docs
    stdin_open: true
    tty: true